#include <CoreServices/CoreServices.r>

resource 'STR#' (126) { {
    "4.0.0.0",
    "Dimdim Publisher for Safari and Firefox on OS X"
} };

resource 'STR#' (127) { {
    "Dimdim Publisher for Safari and Firefox on OS X"
} };

resource 'STR#' (128) { {
    "application/wkdimdim",
	"*"
} };
