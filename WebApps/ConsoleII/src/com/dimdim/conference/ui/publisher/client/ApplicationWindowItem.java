package com.dimdim.conference.ui.publisher.client;

import com.google.gwt.user.client.ui.ClickListener;
import com.google.gwt.user.client.ui.Composite;
import com.google.gwt.user.client.ui.HorizontalPanel;
import com.google.gwt.user.client.ui.Label;
import com.google.gwt.user.client.ui.VerticalPanel;
import com.google.gwt.user.client.ui.Widget;

public class ApplicationWindowItem //extends Composite implements ClickListener
{
/*
	double appHandle;
	String appName;
	boolean isShared = false;
	HorizontalPanel hp =  new HorizontalPanel();
	
	public ApplicationWindowItem(double appHandle, String appName){
		
		initWidget(hp);
		//this.hp.setStyleName("list-entry-panel");
		this.appHandle = appHandle;
		this.appName = appName;
		
		String choppedAppName = "";
		
		if(appName.length() > 80 ){
		    choppedAppName = appName.substring(0, 76) + "...";
		}else{
		    choppedAppName = appName;
		}
		Label appNameLabel = new Label(choppedAppName);
		
		//appNameLabel.setStyleName("console-label");
		appNameLabel.setStyleName("console-share-action");
		//appNameLabel.setWidth("300px");
		//appNameLabel.setWordWrap(false);
		appNameLabel.setTitle(appName);
		
		hp.add(appNameLabel);
		hp.setCellVerticalAlignment(appNameLabel, VerticalPanel.ALIGN_BOTTOM);
		//Label appHandleLabel = new Label(Double.toString(appHandle));
		//hp.add(appHandleLabel);
		//hp.setBorderWidth(1);
		
		//appNameLabel.addClickListener(this);
	}
	
	public void onClick(Widget arg0) {
		//Window.alert("clciked on appHandle = "+appHandle);
		//PublisherInterfaceManager.getManager().startApplicationShare(appHandle);
	}

	public double getAppHandle() {
		return appHandle;
	}

	public String getAppName() {
		return appName;
	}

	public boolean isShared() {
		return isShared;
	}

	public void setShared(boolean isShared) {
		this.isShared = isShared;
	}
	*/
}
